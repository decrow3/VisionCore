# Panel C Joint Observer

Cache-only panel generated from:

```text
/home/declan/VisionCore/outputs/fixation_statistics_by_stimulus_all_sessions_after_review/backimage_trajectory_table_observer_confirm_matched_static_n64_c8_k8_v1/observer_summary.csv
```

At matched-static 1.0x, known-eye accuracy was 1.000,
zero-eye accuracy was 0.328, latent-eye joint accuracy was
0.766 with the empirical trajectory prior and
0.797 with the OU prior. Median N_eff / K was
0.364 for empirical and 0.400
for OU. This panel supports the claim that trajectory marginalization over
exact natural-image response tables recovers image identity lost when the same
moved observation is scored under a zero-eye assumption. Zero-eye here means
the observer assumes zero eye motion; it does not mean the input movie was
static. It does not by itself identify compact geometry as the mechanism.
